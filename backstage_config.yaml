credentials:
  email: ""
  password: ""

max_pages: 1
max_submissions: 10
saved_search: "acting"

submission:
  default_note: ""
  video_reel_ids:
    - 50089085  # new reel
    - 49385977  # IMG_5975
    - 49379289  # IMG_5964
  resume_id: 49686667  # marshall_powell_acting_resume.pdf
  # Cover letter text is hardcoded in src/backstage/main.py SELFTAPE_COVER_LETTER.

browser:
  headless: false

google_calendar:
  calendar_ids:
    - "c50f22709de5e666863424cdec435d33cea633a53fe6cd2f34af591825251187@group.calendar.google.com"
    - "4589f6dc4692352bd5b7c8c39c1f90722f093c1846e3c6185f4b0cf317658d5f@group.calendar.google.com"

logging:
  level: "INFO"
  file: "logs/backstage_auto_apply.log"

# Apply Anyway override flow (shared across all platforms). Must point at the
# same repo/label as config.yaml so Backstage "apply anyway" issues are
# processed and closed on the next run. Requires the OVERRIDE_GITHUB_TOKEN env
# var; comment out to disable.
overrides:
  repo: "powellm4/aa-overrides"
  label: "apply-anyway"
