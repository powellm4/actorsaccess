credentials:
  email: ""
  password: ""

max_pages: 5

# Must match the saved search name on backstage.com exactly. Its filters
# should be: Los Angeles location + Unpaid compensation + Lead/Supporting
# role types. If the name doesn't match, src/backstage/main.py fails closed
# in unpaid mode (raises RuntimeError) so we never silently fall back to
# the default (paid) filters.
saved_search: "unpaid"

submission:
  default_note: ""
  video_reel_ids:
    - 50086411  # IMG_1514
    - 49385977  # IMG_5975
    - 49379289  # IMG_5964
  resume_id: 49686667  # marshall_powell_acting_resume.pdf
  # Cover letter text is hardcoded in src/backstage/main.py SELFTAPE_COVER_LETTER.

browser:
  headless: false

google_calendar:
  calendar_ids:
    - "c50f22709de5e666863424cdec435d33cea633a53fe6cd2f34af591825251187@group.calendar.google.com"
    - "4589f6dc4692352bd5b7c8c39c1f90722f093c1846e3c6185f4b0cf317658d5f@group.calendar.google.com"

logging:
  level: "INFO"
  file: "logs/backstage_auto_apply_unpaid.log"
